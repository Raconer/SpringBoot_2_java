spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/contents?useSSL=false&characterEncoding=UTF-8
    username: root
    password: root
  sql:
    init:
      mode: always
logging:
  level:
    root: info
  charset:
    console: UTF-8